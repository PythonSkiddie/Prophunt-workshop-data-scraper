el scrapo.py runs in a python console, and el scrapo.pyw runs in the background. 
You will have to terminate the process in Task Manager to turn it off.

The default interval between scrapes is 10 minutes.